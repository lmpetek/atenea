<ul>
	<% @calendars.each do |cal| %>
	<li>
		<%= link_to cal.title, :action => :events, :calendar_id => cal.id %>
		(<%= link_to 'X', {:action => :delete_calendar, :calendar_id => cal.id}, :confirm => 'Esta seguro de quere borrar este calendario?' -%>)
	</li>
	<% end %>
</ul>